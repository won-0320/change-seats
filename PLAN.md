# 자리바꾸기 프로그램 (Python + tkinter)

> 개발 기준 문서. 결정 사항 · 설계 · 검증 방법을 담고 있다.
> 최초 작성 2026-07-30 · 행별 열 수 지원 반영

## 현재 진행 상황

**구현 완료.** `python main.py`로 실행된다.

| 파일 | 내용 |
|---|---|
| `main.py` | 실행 진입점 |
| `seatshuffle\model.py` | `Seat`, `Student`, `as_layout()`, `build_seats()`, `seating_grid()`, `seat_total()`, `describe_layout()` |
| `seatshuffle\shuffler.py` | `assign_detailed()` / `assign()` — 셔플 300회 → 백트래킹 |
| `seatshuffle\history.py` | 이력·설정 JSON 입출력, `forbidden_seats()` |
| `seatshuffle\roster.py` | 명단 CSV 읽기, 배정표 CSV 쓰기, `parse_name_line()` |
| `seatshuffle\render.py` | Pillow PNG 렌더, `print_image()` |
| `seatshuffle\ui.py` | tkinter 화면 (행 수 + 행별 열 수 직접 입력) |
| `sample_students.csv` | 16명 예시 명단 |

검증: 배정 로직 14항목 · 파일 입출력 16항목 · 행별 열 수 24항목 · GUI 스모크 33항목 전부 통과.

## Context

교실 자리바꾸기를 매번 손으로 뽑거나 엑셀로 돌리는 대신, 버튼 한 번으로 무작위 배정하고 그 결과를 교실 화면에 띄우고 인쇄까지 할 수 있는 데스크톱 프로그램.

핵심 요구는 두 가지다. **(1) 배정은 순수 무작위**여야 하고(시력·성별·특정 학생 분리 같은 규칙 없음), **(2) 단 하나의 예외로 "지난 회차와 같은 좌석"은 절대 나오지 않아야** 한다. 그래서 회차별 배정 이력을 프로그램이 스스로 저장하고, 다음 배정 때 그 이력을 금지 조건으로 사용한다.

**실제 교실은 행마다 열 수가 다르다.** 5행이고 2행만 4열, 나머지는 3열 → 총 16칸. 이게 기본값이다.

```
        칠  판
    ┌──┐┌──┐┌──┐          1행 3열
┌──┐┌──┐┌──┐┌──┐          2행 4열
    ┌──┐┌──┐┌──┐          3행 3열
    ┌──┐┌──┐┌──┐          4행 3열
    ┌──┐┌──┐┌──┐          5행 3열  = 16칸 / 학생 16명
```

대화로 확정된 결정 사항:

| 항목 | 결정 |
|---|---|
| 실행 형태 | Python GUI 앱 (tkinter) |
| 배정 방식 | 완전 무작위 + 지난 좌석 회피 |
| 회피 강도 | 하드 제약 — 위반 시 재추첨, 못 찾으면 사용자에게 안내 |
| "짝" 회피 | **적용 안 함** (누구 옆에 앉는지는 따지지 않음, 좌석 위치만 회피) |
| 좌석 모양 | **행마다 열 수가 다를 수 있음** (기본 3·4·3·3·3 = 16칸) |
| 교실 입력 | 행 수 + 각 행의 열 수를 모두 키보드로 직접 입력 |
| 짧은 행 정렬 | 가운데 정렬 |
| 명단 입력 | CSV 불러오기 + 화면에서 직접 편집, 둘 다 |
| 인원 ≠ 좌석 수 | 빈칸을 만들지 않고 즉시 안내 후 중단 |
| 결과 활용 | 이력 자동 저장, PNG 저장 / 인쇄, CSV 내보내기 |
| 공개 방식 | 버튼 한 번에 전원 공개 (한 명씩 연출 없음) |
| 수동 드래그 교환 | 범위에서 제외 |

### 확인된 환경

- Python 3.13.9 · tkinter 8.6 · Pillow 12.0.0 — 모두 설치됨, **추가 설치 불필요**
- 한글 폰트 `C:\Windows\Fonts\malgun.ttf`, `malgunbd.ttf` 존재

## 파일 구조

```
C:\Users\user\Desktop\test\
├── main.py               # 실행 진입점 (python main.py)
├── PLAN.md               # 이 문서
├── sample_students.csv   # 16명 예시 명단
├── seatshuffle\
│   ├── model.py          # 좌석·학생·교실 배치(layout)
│   ├── shuffler.py       # 배정 알고리즘
│   ├── history.py        # 이력·설정 JSON
│   ├── roster.py         # CSV 입출력
│   ├── render.py         # PNG 렌더 / 인쇄
│   └── ui.py             # tkinter 화면
└── data\                 # 최초 배정 시 자동 생성
    ├── history.json      # 회차별 배정 이력
    └── settings.json     # 교실 배치, 회피 회차, 명단
```

로직과 화면을 분리해서 GUI 없이도 배정 알고리즘을 단독 테스트할 수 있다.

## 핵심 로직

### 교실 배치 = 행별 열 수 목록 (`model.py`)

교실을 `rows × cols` 두 숫자가 아니라 **행별 열 수 목록(layout)** 으로 표현한다. 이게 이 프로그램의 중심 자료구조다.

```python
Layout = list[int]          # 예: [3, 4, 3, 3, 3] = 5행 16칸

as_layout([3, 4, 3, 3, 3])  # 행마다 열 수가 다른 교실
as_layout(5, 4)             # 5행 × 4열 균일 교실 (같은 함수로 처리)

build_seats([3, 4, 3, 3, 3])   # → Seat 16개. 2행만 4열까지 존재
seat_total([3, 4, 3, 3, 3])    # → 16
seating_grid(seating, layout)  # → 행별 길이가 다른 격자 [3, 4, 3, 3, 3]
describe_layout(layout)        # → "5행 · 3·4·3·3·3열 · 16칸"
```

`Seat(row, col)`은 `frozen=True`(해시 가능)이고 `row=1`이 칠판에서 가장 가까운 줄이다. `Student`도 배정 결과 `dict`의 키로 쓰이므로 `frozen=True`.

`build_seats` / `seating_grid` / `render_png` / `export_seating` / `save_round` 모두 `(layout)` 과 `(행 수, 열 수)` 두 형태를 다 받는다 — 균일 교실도 특별한 경우로 그냥 동작한다.

### 배정 알고리즘 (`shuffler.py`)

2단계. 대부분 1단계에서 끝나고, 조건이 빡빡할 때만 2단계로 간다.

```python
def assign_detailed(students, seats, forbidden, max_attempts=300, rng=None) -> AssignResult:
    # 1단계: 순수 셔플 후 검증 (진짜 균등 무작위)
    for attempt in range(1, max_attempts + 1):
        rng.shuffle(order)
        if 금지 좌석에 아무도 안 걸림:
            return AssignResult(dict(zip(order, seats)), attempt, "shuffle")

    # 2단계: 후보가 가장 적은 좌석부터 채우는 백트래킹 (노드 20,000개 상한)
    return _backtracking_assign(...)
```

- `AssignResult.describe()`가 상태줄에 쓸 "시도 3회" 같은 문구를 만든다
- 둘 다 실패하면 `seating=None` → UI 안내: *"지난 자리를 모두 피하는 배정을 찾지 못했어요. '회피할 회차 수'를 줄이거나 교실 크기를 늘려보세요."*
- `rng` 주입 구조라 테스트에서 `random.Random(42)`로 결과를 고정할 수 있다
- 학생 수 ≠ 좌석 수면 `ValueError` (UI가 그 전에 막지만 방어적으로)

### 이력 (`history.py`)

`data/history.json`:

```json
{
  "rounds": [
    {
      "at": "2026-07-30T14:03:00",
      "rows": 5,
      "layout": [3, 4, 3, 3, 3],
      "seats": { "김민준": [1, 3], "이서연": [2, 1], "박지호": [5, 2] }
    }
  ]
}
```

- `forbidden_seats(history, lookback)` → `{이름: {(행, 열), ...}}`. 기본 `lookback=1`(직전 회차만 회피), GUI에서 0~10 조절
- 이력에 없는 학생(새로 온 학생)은 키가 없어서 어디든 앉을 수 있다
- 교실 배치가 바뀐 회차의 이력도 좌석 좌표 그대로 비교한다 (2행3열은 교실 모양과 무관하게 2행3열)
- 예전 형식(`rows`/`cols`)의 설정 파일도 읽어준다

### CSV 입출력 (`roster.py`)

**불러오기** — 학교에서 쓰는 파일이 제각각이라 유연하게 처리한다.

- 인코딩 `utf-8-sig` → `cp949` 순서로 시도
- 헤더에 `이름`/`성명`/`name` 열이 있으면 그 열, 없으면 첫 열. `번호`/`출석번호` 열도 함께 읽음
- 헤더가 없고 첫 열이 전부 숫자면 번호 열로 판정
- 빈 줄은 건너뛰고, 동명이인은 경고 (이력이 이름 기준이므로 `김민준2`처럼 구분 필요)
- `parse_name_line("1. 김민준")` → 붙여넣기용. 번호가 붙어 있어도 알아본다

**내보내기** — `utf-8-sig` CSV. 짧은 행의 남는 칸은 빈칸:

```
행,1열,2열,3열,4열
1행,오건우,안다은,장수아,
2행,정준서,신유진,강예린,임시우
3행,김민준,권태윤,최하은,
```

### PNG 렌더링 & 인쇄 (`render.py`)

tkinter Canvas 캡처는 화면 상태에 의존해 불안정하므로 **Pillow로 처음부터 다시 그린다.**

- 폰트 `malgun.ttf`(제목은 `malgunbd.ttf`) → `gulim.ttc` → 기본 폰트로 폴백
- 제목 + 날짜·회차, "칠판" 띠, 좌석 카드(번호 + 이름)
- 이미지 폭은 **가장 넓은 행** 기준으로 계산하고, 짧은 행은 가운데 정렬
- 이름이 길면 셀 폭에 맞춰 글자 크기를 줄임
- 인쇄: `os.startfile(path, "print")` → 실패하면 저장 폴더를 열고 직접 인쇄 안내

## 화면 구성 (`ui.py`)

```
┌ 자리바꾸기 ─────────────────────────────────────────────────[X]┐
│ 파일  도구                                                     │
│ ┌─ 명단 (16명) ────┐  ┌─ 배치 결과 ───────────────────────────┐ │
│ │  1  1. 김민준    │  │        ▔▔▔▔▔▔ 칠 판 ▔▔▔▔▔▔          │ │
│ │  2  2. 이서연    │  │    ┌────┐┌────┐┌────┐                │ │
│ │  3  3. 박지호  ▼ │  │    │오건우││최하은││황서준│           │ │
│ └──────────────────┘  │  ┌────┐┌────┐┌────┐┌────┐            │ │
│ [        ] [추가]     │  │김민준││이서연││강예린││장수아│      │ │
│ [붙여넣기][삭제][비우기]│    ┌────┐┌────┐┌────┐              │ │
│ [   CSV 불러오기    ] │    │신유진││임시우││서지안│           │ │
│                       │      ... (5행까지)                    │ │
│ ┌─ 교실 ────────────┐ │                                       │ │
│ │ 행 수 [5] (1~15)  │ │  2026-07-30 · 1회차 배정              │ │
│ │ 행마다 열 수를 직접 입력                                     │ │
│ │ 1행[3]열  2행[4]열 │ │      · 5행 · 3·4·3·3·3열 · 16칸      │ │
│ │ 3행[3]열  4행[3]열 │ └───────────────────────────────────────┘ │
│ │ 5행[3]열           │                                          │
│ │ 3·4·3·3·3 = 16칸 · 명단 16명 (딱 맞음)   ← 초록/빨강         │ │
│ └───────────────────┘                                          │
│ ┌─ 지난 자리 회피 ──┐                                          │
│ │ 최근 [1] 회차 회피 │                                          │
│ └───────────────────┘                                          │
│ [자리 배정] [PNG 저장] [인쇄] [CSV 내보내기]  상태: 배정 완료... │
└────────────────────────────────────────────────────────────────┘
```

### 교실 입력 (행 수 + 행별 열 수 직접 입력)

- **행 수** 입력창(`ttk.Entry`)에 숫자를 타이핑하면 그 개수만큼 **행별 열 수 입력창이 자동으로 생기거나 줄어든다.** 기존에 입력한 값은 최대한 유지된다
- 입력창은 전부 키보드 직접 입력. 숫자만, 최대 2자리. 행 수 `1~15`, 각 행 열 수 `1~15`
- 고칠 때마다 `3·4·3·3·3 = 16칸 · 명단 16명 (딱 맞음)` 을 즉시 계산해 **초록(일치) / 빨강(불일치)** 으로 표시
- 값이 바뀌면 오른쪽 격자가 그 모양의 빈 칸으로 즉시 갱신 (이전 배정 결과는 지워짐)
- 비었거나 범위를 벗어나면 배정 시점에 몇 행이 문제인지 알려준다
- `도구 > 모든 행을 같은 열 수로...` — 균일한 교실로 한 번에 맞추는 편의 기능

### 그 외

- **명단 편집**: Listbox(다중 선택) + 입력창. `추가`(쉼표로 여러 명), `붙여넣기`(한 줄에 한 명), `삭제`, `비우기`, `CSV 불러오기`
- **회피할 회차**: `ttk.Spinbox` 0~10, 기본 1. `0`이면 회피 없이 순수 무작위
- **자리 배정**: 인원 검증 → 배정 → 전원 한 번에 표시 → 이력 자동 저장. 상태줄에 시도 횟수·회차
- **결과 격자**: Canvas. 칠판 띠로 앞쪽을 명시, 짧은 행은 가운데 정렬, 창 크기에 맞춰 셀 크기 자동 조절
- 교실 배치·회피 회차·명단은 종료 시 `data/settings.json`에 저장되어 다음 실행에 복원된다
- `도구 > 배정 이력 초기화` — 새 학기·새 반일 때 리셋 (확인 대화 후)

## 검증 방법

**1) 행별 열 수 교실** — 좌석 생성·격자·PNG 폭·CSV 빈칸까지

```powershell
python -c "
from seatshuffle.model import build_seats, seat_total, describe_layout
L = [3, 4, 3, 3, 3]
print(describe_layout(L))                      # 5행 · 3·4·3·3·3열 · 16칸
print(seat_total(L))                           # 16
print([s.label() for s in build_seats(L) if s.row == 2])   # 2행만 4열까지
"
```

**2) 지난 자리 회피**

```powershell
python -c "
from seatshuffle.model import Student, build_seats
from seatshuffle.shuffler import assign
import random
students = [Student(f'학생{i}') for i in range(1, 17)]
seats = build_seats([3, 4, 3, 3, 3])
a1 = assign(students, seats, {}, rng=random.Random(1))
forbidden = {s.name: {(seat.row, seat.col)} for s, seat in a1.items()}
a2 = assign(students, seats, forbidden, rng=random.Random(2))
print('겹침:', sum(1 for s in students if a1[s] == a2[s]))
"
```
→ `겹침: 0`

**3) 무작위성** — 이력 없이 1600번 배정해, 2행 4열(유일한 4열 자리)에 16명이 골고루 앉는지 확인 (각 100회 근처)

**4) 실패 경로** — `회피할 회차`를 크게 올려 이력을 쌓은 뒤, 해가 없을 때 크래시 없이 안내가 뜨는지

**5) GUI 전체 흐름**

```powershell
python main.py
```
- `CSV 불러오기` → `sample_students.csv` → 16명 확인
- 기본 교실이 행 5 / 3·4·3·3·3 = 16칸이고 초록색 `(딱 맞음)` 인지 확인
- `자리 배정` → 1행 3명, 2행 4명, 3~5행 3명씩, 짧은 행이 가운데 정렬되는지
- 한 번 더 배정 → 지난 자리와 겹치는 학생이 없는지 (`data/history.json` 두 회차 비교)
- 행 수를 6으로 바꿔보기 → 6행 입력창이 생기고 결과가 초기화되는지
- 3행을 4열로 바꿔보기 → `17칸 · 명단 16명 (1칸이 남음)` 빨간색, 배정 시 안내 후 중단
- `PNG 저장` / `CSV 내보내기` → 한글 정상, 짧은 행 빈칸 확인
- 앱을 닫고 다시 실행 → 교실 배치와 명단이 복원되는지
- `인쇄` → 인쇄 대화가 뜨는지

## 범위에서 제외한 것

나중에 필요하면 추가할 수 있는 구조로 남겨뒀다.

- 시력·키 배려, 특정 학생 분리, 성별 균형 등 배정 규칙 (`shuffler.py`의 `forbidden` 인자를 확장하면 붙는다)
- 한 명씩 공개하는 추첨 연출
- 배정 후 드래그로 두 학생 자리 교환
- 모둠형·빈칸이 중간에 뚫린 교실 (지금은 각 행이 왼쪽부터 연속으로 채워진 모양만)
- exe 패키징 (필요해지면 PyInstaller로 별도 작업)
